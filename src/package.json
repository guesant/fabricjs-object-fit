{
  "name": "@guesant/fabricjs-object-fit.repo.src",
  "private": true,
  "scripts": {
    "build": "pnpm exec nx run fabricjs-object-fit:build",
    "dev": "pnpm exec nx run fabricjs-object-fit:dev",
    "test": "pnpm exec nx run fabricjs-object-fit:test",
    "test:e2e": "pnpm exec nx run fabricjs-object-fit:test:e2e",
    "lint": "pnpm exec nx run fabricjs-object-fit:lint",
    "lint:fix": "pnpm exec nx run fabricjs-object-fit:lint:fix",
    "format": "biome format --write .",
    "docs:dev": "pnpm exec nx run docs:dev",
    "docs:build": "pnpm exec nx run docs:build",
    "docs:preview": "pnpm exec nx run docs:preview"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/node": "^25.9.2"
  }
}